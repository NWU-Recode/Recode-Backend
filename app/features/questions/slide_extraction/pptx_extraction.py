# Stub for extract_pptx_text to prevent import errors

def extract_pptx_text(data):
    """
    Dummy function for extracting text from pptx files.
    Returns a dict with slide numbers as keys and lists of strings as values.
    """
    return {1: ["Slide 1 text"], 2: ["Slide 2 text"]}
